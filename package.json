{
  "name": "koto-evaluation",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "build": "npm run build --prefix client && vercel build",
    "start": "vercel"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}